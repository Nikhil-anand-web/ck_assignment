/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com','localhost','fakestoreapi.com','encrypted-tbn3.gstatic.com',"unixindia.in"],
      },
};

export default nextConfig;
